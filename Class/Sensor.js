// ./Sensor.js
class Sensor {
  constructor(sensor_id, name, type, floor_number) {
    this.sensor_id = sensor_id;
    this.name = name;
    this.type = type;
    this.floor_number = floor_number;
  }

  // Other methods or properties of the Sensor class can be defined here
}

// Export the Sensor class
module.exports = Sensor;
